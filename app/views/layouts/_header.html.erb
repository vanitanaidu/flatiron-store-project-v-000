<nav class="">
    <%= render 'layouts/signin' %>

      <h2> <%= link_to "Store Home", store_path %> </h2>

        <% if user_signed_in? && !!current_user.current_cart %>
           <%= link_to "Cart", cart_path(current_user.current_cart) %>
        <% end %>

</nav>
